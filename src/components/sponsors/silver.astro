---
const CoHost = [
    {
        name: "",
        src: "/sponsors/kapsriyadi.png",
    },
    {
        name: "",
        src: "/sponsors/sayurbox.png",
    },
];
---

<div class="m-4 w-full mx-auto">
    <div class="flex items-center justify-center mt-2 mb-3 w-full">
        <h2
            class="rounded-full bg-ncafa-3 text-white text-base sm:text-2xl lg:text-3xl lg:text-center px-6 py-1"
        >
            Silver Sponsors
        </h2>
    </div>
    <div
        class="w-3/4 lg:w-1/2 grid grid-cols-2 justify-center justify-items-center md:grid-cols-2 gap-2 py-4 m-auto"
    >
        {
            CoHost.map((cohost) => (
                <div class="flex justify-center">
                    <img
                        class="h-auto w-3/4 rounded-lg"
                        src={cohost.src}
                        alt={cohost.name}
                    />
                </div>
            ))
        }
    </div>
</div>
